class Solution(object):
    def isPalindrome(self, x):
        """
        :type x: int
        :rtype: bool
        This is a simple code that checks if a number is a palindrome 
        by converting it to a string
        
        """
        num = str(x)
        rev_num = num[::-1]
        return num == rev_num
